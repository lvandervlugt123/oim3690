# to do list
- continue working on personal site
- feedback from project 1 
- proposal for project 2

# to do analysis
- Where is the script tag?
    it’s at the very bottom of the page, right before body
    The browser loads HTML top → bottom
    By putting script at the bottom, all HTML elements already exist
    So JavaScript can safely “find” them

- How does JavaScript find HTML elements?
    js uses getelement which essentially means Find the element with this ID so I can use it

- what makes the "add" button actually do something?
    When the button is clicked:

        Get text from input
        Add it to the todos array
        Save it
        Re-render the page

# continuation of Java Script basics
- You changed a heading's color in the console and it worked. After refreshing the page, its gone. Why?
    this is because you didnt change the actual html file, the console only makes temporary pages. 
    refreshing a page is simply re-requesting the html file from the server and re downloading the page

- one of the major benefits of using variables is making your code much cleaner, less hard to read
- very important in js to create variable names
- in js if you expect the variabkes value to change in the future you use let, you use const for values that stay the same